{
  "engines": {
    "node": ">=20"
  },
  "name": "invoice-builder",
  "version": "1.0.0",
  "description": "Professional Invoice Builder",
  "main": "index.js",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./input.css -o ./output.css",
    "start": "npx @tailwindcss/cli -i ./input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.2"
  }
}
